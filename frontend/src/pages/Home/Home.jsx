import React, {useState} from 'react'
import { PlusIcon, BookOpenIcon, FolderIcon } from 'lucide-react'

const Home = ({ categories, decks, onCreateDeck, onSelectDeck}) => {
  return (
    <div>This is the homepage</div>
  )
}

export default Home